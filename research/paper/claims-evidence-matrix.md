# Claims-to-evidence matrix

This file is the source of truth for manuscript language. “Supported” means the checked-in evidence is sufficient for the exact wording shown—not that the finding automatically generalizes beyond Kaetram.

| Claim | Current evidence | Status | Allowed wording now | Required upgrade |
|---|---|---|---|---|
| r10 SFT performs below base | Base run totals `[7,7,7,7]`; SFT `[3,1,2]`; n=4 versus n=3 runs; one-sided exact Mann–Whitney p=0.0286; Foresting 8/12 versus 1/9, Fisher p=0.0159 | Exploratory | “All four observed base runs exceeded all three SFT runs.” | Restore raw artifacts, predeclare the test, use run-level replication, report multiplicity and CIs |
| r10 is a 3.5× regression | Mean Core-3 stages 7 versus 2 | Descriptive | “Mean score fell from 7 to 2 in seven exploratory runs.” | Same as above; do not use the ratio as a general effect estimate |
| Corpus prior becomes inference prior | Aggregate decision-verb frequencies match within about 1pp for several verbs | Hypothesis, not mechanism | “The aggregate tool-use fingerprint is consistent with marginal imitation.” | Matched-state action probabilities, opportunity-normalized recall, and a corrective reweighting intervention |
| SFT and base share the same typed interface | They share seventeen command names and the MCP execution backend, but base receives native `tools=` schema rendering while SFT does not; the manual schema is incomplete | Refuted as written | “The models share a seventeen-command execution vocabulary and backend.” | Byte-identical schema serialization and full-schema hash parity across train and every eval arm |
| Scaffold dominates capacity | R11 scouting: 2B 12–13, 4B 12/17, 9B 12–19, 27B 15, with differing durations and n=1–2 per size | Scouting observation | “In our small capacity sweep, larger models did not monotonically improve Core-3 score.” | Matched durations, repeated seeds, uncertainty, and a prespecified capacity analysis |
| Round one transfers style without competence | Base and r1 both 12/30; tool mix, tempo, and per-verb KL move toward teacher | Descriptive, n=1 arm | “Round one changed measured behavior without changing the run-level benchmark score.” | Independent replications and a fixed held-out state set |
| Environment-state seeding transfers competence | Base/r1 0/3 and r2 3/3 on the Herbalist wall; r2 15/30 versus base 12/30; evaluation loaded no intermediate state, but its gameplay-RNG seed was not preserved | Strongest observational sequence; causal test pending | “The seeded-training round was followed by unanimous canonical-start wall passage in one three-agent run.” | Otherwise identical state-augmented versus natural-only training arms, registered environment/inference seeds, and independent run-level replication |
| Reachability-targeted persistent-player-state initialization improves OPD | Protocol and prospective selection/seeding code only; the historical milestone was hand-selected and checked for loadability/walkability, not legally certified. PR #45 provides an executable isolated MCP/Mongo reachability checker, PR #48 enumerates 50 core arm–seed cells plus 20 separately reported state–history cells, and stacked PR #49 hash-verifies and normalizes all registered record types without running a trainer. PR #51 freezes the shared LoRA parameterization and adds an unexecuted direct-token corrected-interface SFT path; PR #50 prepares SCoRe-style Stage 1 but blocks Stage 2. PR #52 validates the Guided role schedule and complete mixed-history record contract, then blocks because the live collector and asymmetric objective backend are absent. No live reachability certificate, verified arm bundles, Guided execution, accelerator run, or matched outcome exists yet. | Unsupported outcome; testable hypothesis | “We specify a frozen selector and matched control suite.” | Run and preserve a live isolated reachability replay, regenerate candidate trials, materialize verified arm bundles, implement and execute the Guided mixed-turn/asymmetric-KL path and SCoRe-style Stage 2, run matched natural/random/progress/TCOD/Guided arms, state-by-history ablations, independent canonical-start runs with registered RNG seeds, and run-level uncertainty |
| Round two is a pure weights improvement | R2 evaluation reportedly used recovery off, but exact historical schema, prompt, checkpoint, seed, and environment parity cannot be reconstructed | Reported recovery-off contrast; causal weights attribution unavailable | “Round two without recovery was reported at 15/30 versus the base run's 12/30; exact parity and independent reproduction remain unavailable.” | Replicate under frozen manifests and the canonical render contract |
| Round three reaches 18/30 through weights | R3 used a new tool-recovery affordance; r2 weights plus recovery score 17/30 | Refuted as written | “The final weights-plus-recovery system reached 18/30.” | Full weights × recovery factorial; never call 18/30 pure weights |
| Teacher-forcing copy prior | Historical notes report teacher logp -0.161 versus student -0.253 for one malformed continuation after malformed history; the “correct-form” comparison uses a different state and the probe artifact is absent | Post-hoc secondary diagnostic; no matched preference reversal | “One targeted state reportedly gave a malformed continuation positive teacher-over-student distillation advantage.” | Four matched cells (clean/malformed history × canonical/malformed candidate), identical raw strings/tokenization, multiple defects/models, paired uncertainty, and preserved inputs/outputs |
| Harness recovery cures the format defect | Historical notes report 405 recovery markers and no repeated marker within rewritten sessions; source logs and raw pre-rewrite emissions are absent | Narrative proxy only | “Historical notes report one recovery marker in each of 405 rewritten sessions.” | Preserve raw pre-rewrite emissions and replicate under frozen logging; do not infer model correction or relapse prevention |
| The teacher cannot transfer cooking | 4B teacher and post-Herbalist student make zero `craft_item` calls in the audited runs | Conditional null | “No cooking behavior was observed from the same-family teacher in these runs.” | A competent-teacher arm; do not infer a universal impossibility |
| MCP is novel in game-agent training | Orak uses MCP across twelve games and includes a fine-tuning dataset | Refuted | Do not claim | Position MCP as infrastructure, not novelty |
| Capability archetype diversity improves training | No one-versus-three ablation | Unsupported | “We collect three prompt variants.” | Direct one-versus-three data/training ablation |
| KTO improves the agent | KTO entry point is a planning stub; only an old ten-step smoke test is described | Unsupported | “KTO was considered but not evaluated.” | Implemented, preserved, controlled SFT versus SFT+KTO experiment |
| World model improves play | Deprecated code, no evaluation | Unsupported | Omit | Full evaluation and planning-impact ablation |
| Continual learning | Repeated rounds on one task, no sequential-task retention protocol | Unsupported | Omit | Sequential skill acquisition plus retention/forgetting evaluation |
| Cross-environment generality | One game | Unsupported | “Single-environment case study.” | Held-out quest plus second environment |

## Reproducibility audit

The current artifact fails the paper's own reproduction bar:

- The dedicated `run-eval.sh` servers used `kaetram_eval` from April 25 onward while the wrapper reset and harness snapshots targeted `kaetram_devlopment`. Results from that dedicated lane are quarantined pending raw proof of clean starts or reruns. The headline r10 and June OPD Core-3 values use the separate orchestrator path, whose server and reset both target `kaetram_devlopment`, so PR #37's specific mismatch does not directly implicate those values. Missing raw bundles still prevent independent reproduction.
- `dashboard/db.py` keys quest-stage snapshots by internal quest ID, while the historical `eval_harness.py` path looked up display names. A deterministic fixture could score completed internal `foresting` as zero. Draft PR #53 corrects the mapping and covers all 3+3+4 stages; prospective Core-3 results remain blocked until that fix is merged and deployed.
- `python3 scripts/r10_stats.py` crashes with `KeyError: 'agent_0'` in both the clean clone and the original local checkout.
- The r10 raw logs and generated SFT dataset are absent from the repository.
- OPD gate summaries are checked in, but their input records and fixed external test set are absent.
- The round-two and round-three gates report `0/0` parameter-emitting completions, so the format check is vacuous.
- Several round-three “non-degenerate” samples are irrelevant prose, code, refusals, or fake tool syntax; the checker detects repetition, not task validity.
- Draft PR #46 adds an exact Python 3.12 lock and safe clean-clone bootstrap; a fresh temporary clone passed 143 CPU unit tests with 31 documented asset/service skips. This proves prospective local unit collection only, not training, live gameplay, checkpoints, historical results, or table regeneration.
- `eval_harness.py` reads `SCENARIOS[scenario]["max_turns"]` while current scenarios define `duration_minutes`, which can crash result saving.
- Base and SFT rendering differ: base passes native `tools=` schemas to the Qwen template; SFT intentionally omits them.
- Arena PR #47 carries deterministic per-replicate inference and environment seeds and verifies a game startup attestation. Kaetram-Open draft PR #333 centralizes server/common gameplay draws and rejects pre-seed draws, but it is not merged, deployed, or verified in a live run. Confirmatory launch therefore remains fail-closed.
- The factorial launcher now seals resolved prompts, exact pre-rewrite endpoint emissions, parsed tool-transition transcripts, state-boundary snapshots, launcher/results files, and an exact requested/completed-cell inventory. This prospective contract has not yet produced a live bundle.
- The prospective state seeder restores persistent player state, not the complete shared world. Manuscript claims are therefore limited to player-state initialization unless NPC, mob, resource, concurrent-player, clock, and environment-RNG state are also sealed and restored.

No main-paper table should be labeled reproducible until a clean, documented environment regenerates it from a versioned artifact bundle.
