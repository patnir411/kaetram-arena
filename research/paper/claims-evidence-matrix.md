# Claims-to-evidence matrix

This file is the source of truth for manuscript language. “Supported” means the checked-in evidence is sufficient for the exact wording shown—not that the finding automatically generalizes beyond Kaetram.

| Claim | Current evidence | Status | Allowed wording now | Required upgrade |
|---|---|---|---|---|
| r10 SFT performs below base | Base run totals `[7,7,7,7]`; SFT `[3,1,2]`; n=4 versus n=3 runs; one-sided exact Mann–Whitney p=0.0286; Foresting 8/12 versus 1/9, Fisher p=0.0159. The recovered external bundles are bound to inventory SHA-256 `e954237243b9928a6d8096fbabb279b52bc93f0adc977e300e76e7eea40ae85d`; the checked-in run-digest audit independently hashes 60 selected directories totaling 18,812 files, and both analyses rerun from those paths. | Exploratory | “All four observed base runs exceeded all three SFT runs.” | Release the recovered inputs under a reviewable artifact contract, predeclare the test, use run-level replication, report multiplicity and CIs |
| r10 is a 3.5× regression | Mean Core-3 stages 7 versus 2 | Descriptive | “Mean score fell from 7 to 2 in seven exploratory runs.” | Same as above; do not use the ratio as a general effect estimate |
| Corpus prior becomes inference prior | Aggregate decision-verb frequencies match within about 1pp for several verbs | Hypothesis, not mechanism | “The aggregate tool-use fingerprint is consistent with marginal imitation.” | Matched-state action probabilities, opportunity-normalized recall, and a corrective reweighting intervention |
| SFT and base share the same typed interface | They share seventeen command names and the MCP execution backend, but base receives native `tools=` schema rendering while SFT does not; the manual schema is incomplete | Refuted as written | “The models share a seventeen-command execution vocabulary and backend.” | Byte-identical schema serialization and full-schema hash parity across train and every eval arm |
| Scaffold dominates capacity | R11 scouting: 2B 12–13, 4B 12/17, 9B 12–19, 27B 15, with differing durations and n=1–2 per size | Scouting observation | “In our small capacity sweep, larger models did not monotonically improve Core-3 score.” | Matched durations, repeated seeds, uncertainty, and a prespecified capacity analysis |
| Round one transfers style without competence | Base and r1 both 12/30; tool mix, tempo, and per-verb KL move toward teacher | Descriptive, n=1 arm | “Round one changed measured behavior without changing the run-level benchmark score.” | Independent replications and a fixed held-out state set |
| Environment-state seeding transfers competence | Base/r1 0/3 and r2 3/3 on the Herbalist wall; r2 15/30 versus base 12/30; evaluation loaded no intermediate state, but its gameplay-RNG seed was not preserved | Strongest observational sequence; causal test pending | “The seeded-training round was followed by unanimous canonical-start wall passage in one three-agent run.” | Otherwise identical state-augmented versus natural-only training arms, registered environment/inference seeds, and independent run-level replication |
| The July mechanism-arm scores are real historical observations | All 27 agent/run bundles for nine six-hour arms were recovered read-only and copy-verified against source manifest SHA-256 `d166fa1b6bf46cb908d32cb4e14d5ae8215f9377d2aae1b12532f1aa183741a8`. `research/audits/july-mechanism-run-digests.json` binds 19,005 source files. Before scoring, `july-mechanism-results.json` verifies a clean analysis-code receipt, rehashes every scored directory against that manifest, and admits the historical naive clock as UTC only after all 27 monotonic event streams begin 260–422 seconds after their offset-aware run starts. Record-level replay reproduces arm totals `[12,18,12,15,13,14,12,17,14]`. The checked-in anonymous projection contains 21,524 score-relevant observations, binds each row to its source record/log, and regenerates those totals from a clean checkout (artifact SHA-256 `26f0f7f8e22757b1af1e90af93e9baf71969b32e7bb77e847a6c5b54ddb5818d`). | Supported as content-bound, reviewer-replayable descriptive scores; not as treatment effects or an independent raw-extraction audit | “Content-bound record-level replay reproduces the nine reported July scores at their six-hour boundaries.” | Release the full licensed raw bundle or permit an independent extraction audit; attest exact checkpoint, training corpus, reset, game revision, render contract, and both RNG seeds; repeat independent training runs |
| Teacher-graded OPD outperforms uniform self-imitation in the recovered intended-pair family | Four post-hoc pairs labelled corpus/init-matched in historical notes have graded totals 12/12/15/18 and uniform totals 13/14/15/17; every cell is one training run, matching variables are unattested, and execution eras/configuration differ. A repaired-render graded round-1 sensitivity scores 14 versus the old-render uniform arm's 13, reversing the first intended pair while changing the render contract. | No advantage established; descriptive audit only | “Across four one-run intended pairs, uniform-weighted controls matched or exceeded the graded arm in three cells; a repaired-render sensitivity reverses one direction, and the design cannot estimate a grading effect.” | Fully crossed, independently replicated training seeds with immutable corpus/checkpoint/config receipts and nested evaluation seeds |
| Reachability-targeted persistent-player-state initialization improves OPD | Protocol and prospective selection/seeding code only; the historical milestone was hand-selected and checked for loadability/walkability, not legally certified. PR #45 provides an executable isolated MCP/Mongo reachability checker, PR #48 enumerates 50 core arm–seed cells plus 20 separately reported state–history cells, and stacked PR #49 hash-verifies and normalizes all registered record types without running a trainer. PR #51 freezes the shared LoRA parameterization and adds an unexecuted direct-token corrected-interface SFT path; PR #50 prepares SCoRe-style Stage 1 but blocks Stage 2. PR #52 validates the Guided role schedule and complete mixed-history record contract, then blocks because the live collector and asymmetric objective backend are absent. TRB is identified as a required occupancy baseline but is not implemented. No live reachability certificate, verified arm bundles, Guided/TRB execution, accelerator run, or matched outcome exists yet. | Unsupported outcome; testable hypothesis | “We specify a frozen selector and matched control suite.” | Run and preserve a live isolated reachability replay, regenerate candidate trials, materialize verified arm bundles, implement and execute the Guided mixed-turn/asymmetric-KL path, TRB, and SCoRe-style Stage 2, run matched natural/random/progress/TCOD/Guided/TRB arms, state-by-history ablations, independently trained checkpoints with nested canonical-start evaluation seeds, and method-level uncertainty |
| Round two is a pure weights improvement | R2 evaluation reportedly used recovery off, but exact historical schema, prompt, checkpoint, seed, and environment parity cannot be reconstructed | Reported recovery-off contrast; causal weights attribution unavailable | “Round two without recovery was reported at 15/30 versus the base run's 12/30; exact parity and independent reproduction remain unavailable.” | Replicate under frozen manifests and the canonical render contract |
| The registered local evaluation cells execute with retained integrity evidence | Preregistered nine-cell feasibility pilot: Base/R2/R3 × three paired inference/environment seeds, one five-minute recovery-off completionist episode per cell. All 9 cells completed; 261 files in sealed per-cell inventories rehashed without mismatch; exact canonical-start projections and seed-bound RNG receipts present. Across 106 raw generations, 42 emitted a structured call and 64 emitted none; all 42 calls had canonical names and JSON-object arguments. Retained stderr contains no logged `API error:` line. No quest-stage progress occurred. Bundle-index SHA-256 `cf62c370b0e5d133f3049b6aab3c114d4795e5a9ed97dc4f68807ff4f32f1c26`. | Supported as prospective execution/integrity evidence only; independent reproducibility is not established | “A nine-cell local pilot executed all registered cells with internally hash-checked evidence; no cell advanced a quest.” | Release the complete bundle, regenerate from a clean clone, and run independently powered long-horizon cells before any policy-quality claim |
| The 30-minute local weights × recovery diagnostic engages recovery | Preregistered Base/R2/R3 × recovery off/on × three paired seeds. All 18 cells were launcher-valid and 1,266 sealed files rehashed. Across 958 generations, 294 contained canonical structured calls, 664 contained none, and zero were malformed or recovery-eligible. Four cells advanced one quest/Core-3 stage. Bundle-index SHA-256 `c6c69bbf3416cf8405c211032b5c1e3fdc250c264325faed69bf618e941aab0e`. | Manipulation-check failure; recovery effect unidentified; checkpoint patterns descriptive only | “The registered local factorial completed, but the recovery intervention never engaged because no eligible defect occurred.” | Validate natural trigger incidence before the full factorial; separately preregister controlled defect injection if conditional recovery efficacy is a research question; release the raw bundle and independently regenerate the export |
| Native tool-schema exposure changes dropped executable-call incidence | The preregistered fixed-state grid completed all 1,200 local requests with zero failures and stable Base/R2/R3 checkpoint identities. A byte-identical rerun reproduced the sealed analysis; a separately implemented auditor recomputed all labels, 12 cells, and nine contrasts from the checked-in raw bundle (artifact-index SHA-256 `fe117a98c506be441be12c07e4f467b00751807ee8f473e8026998fa257c1560`). The five nominal request seeds produced one semantic response in every state-condition group, so duplicate replays collapse to 20 state outputs per cell. Native-schema contrasts are +30, +22.5, and +15 percentage points; documentation contrasts are 0, -7.5, and +10. | Supported as a descriptive, checkpoint-conditional interface result on the fixed grid; no stochastic uncertainty, training ranking, or recovery benefit | “Under the tested renderer, native schema exposure increased executable calls emitted into text without a structured tool call by 15–30 points across the three fixed checkpoints.” | Repair and attest per-request sampling diversity; replicate on independent state pools, renderers, and model families; separately test whether executing recovered calls improves state transitions |
| Round three reaches 18/30 through weights | The June r3 run used recovery. A recovered July run labelled `2b-opd-r3-norec` independently re-scores to 18/30 with recovery disabled, but its checkpoint digest, render/config receipt, reset receipt, and seeds are missing | Strong descriptive recovery-off observation; pure-weights attribution still unavailable | “A later run labelled round-3 with recovery disabled also reached 18/30.” | Attested checkpoint identity and a replicated weights × recovery factorial with actual recovery opportunities |
| Teacher-forcing copy prior | Historical notes report teacher logp -0.161 versus student -0.253 for one malformed continuation after malformed history; the “correct-form” comparison uses a different state and the probe artifact is absent | Post-hoc secondary diagnostic; no matched preference reversal | “One targeted state reportedly gave a malformed continuation positive teacher-over-student distillation advantage.” | Four matched cells (clean/malformed history × canonical/malformed candidate), identical raw strings/tokenization, multiple defects/models, paired uncertainty, and preserved inputs/outputs |
| Harness recovery cures the format defect | Historical notes report 405 recovery markers and no repeated marker within rewritten sessions. Recovered rewritten session bundles exist, but raw pre-rewrite emissions and an independently regenerated marker count are absent. | Narrative proxy only | “Historical notes report one recovery marker in each of 405 rewritten sessions.” | Preserve raw pre-rewrite emissions, regenerate the count, and replicate under frozen logging; do not infer model correction or relapse prevention |
| The teacher cannot transfer cooking | 4B teacher and post-Herbalist student make zero `craft_item` calls in the audited runs | Conditional null | “No cooking behavior was observed from the same-family teacher in these runs.” | A competent-teacher arm; do not infer a universal impossibility |
| MCP is novel in game-agent training | Orak uses MCP across twelve games and includes a fine-tuning dataset | Refuted | Do not claim | Position MCP as infrastructure, not novelty |
| Capability archetype diversity improves training | No one-versus-three ablation | Unsupported | “We collect three prompt variants.” | Direct one-versus-three data/training ablation |
| KTO improves the agent | KTO entry point is a planning stub; only an old ten-step smoke test is described | Unsupported | “KTO was considered but not evaluated.” | Implemented, preserved, controlled SFT versus SFT+KTO experiment |
| World model improves play | Deprecated code, no evaluation | Unsupported | Omit | Full evaluation and planning-impact ablation |
| Continual learning | Repeated rounds on one task, no sequential-task retention protocol | Unsupported | Omit | Sequential skill acquisition plus retention/forgetting evaluation |
| Cross-environment generality | One game | Unsupported | “Single-environment case study.” | Held-out quest plus second environment |

## Reproducibility audit

The current artifact fails the paper's own reproduction bar:

- The dedicated `run-eval.sh` servers used `kaetram_eval` while the wrapper reset and harness snapshots targeted `kaetram_devlopment`. The conservative main-branch exposure window is April 25 through PR #37's July 22 merge; a fixed branch existed from July 18. Results from that dedicated lane are quarantined unless they attest the fixed branch or prove clean starts. The headline r10 and June OPD Core-3 values use the separate 9001/9011/9021 orchestrator path, so PR #37's specific mismatch does not directly implicate them. Their recovered first model-visible states are canonical, but the exact historical reset command and server revision remain unattested.
- `dashboard/db.py` keys quest-stage snapshots by internal quest ID, while the historical `eval_harness.py` path looked up display names. A deterministic fixture could score completed internal `foresting` as zero. Draft PR #53 corrects the mapping and covers all 3+3+4 stages; prospective Core-3 results remain blocked until that fix is merged and deployed.
- The r10 credit and comparison analyses rerun successfully when pointed at the recovered external paths, reproducing base `[7,7,7,7]`, SFT `[3,1,2]`, exact Mann–Whitney `p=0.0286`, and Foresting 8/12 versus 1/9. A clone without the mounted recovery cannot regenerate them.
- The r10 and full July raw logs are not checked into or released with the repository. The recovered run directories are content-bound by `research/audits/historical-run-digests.json` and `research/audits/july-mechanism-run-digests.json`; the latter deterministically regenerates the nine protocol-boundary scores in `research/audits/july-mechanism-results.json`. `research/artifacts/july-score-replay-v1/` is a reviewer-accessible, anonymous score projection with source-record/log bindings and an exact clean-clone replay command. It supports score verification but not independent repetition of the private raw-to-projection extraction.
- OPD gate summaries are checked in, but their input records and fixed external test set are absent.
- The round-two and round-three gates report `0/0` parameter-emitting completions, so the format check is vacuous.
- Several round-three “non-degenerate” samples are irrelevant prose, code, refusals, or fake tool syntax; the checker detects repetition, not task validity.
- Draft PR #46 adds an exact Python 3.12 lock and safe clean-clone bootstrap; a fresh temporary clone passed 143 CPU unit tests with 31 documented asset/service skips. This proves prospective local unit collection only, not training, live gameplay, checkpoints, historical results, or table regeneration.
- `eval_harness.py` reads `SCENARIOS[scenario]["max_turns"]` while current scenarios define `duration_minutes`, which can crash result saving.
- Base and SFT rendering differ: base passes native `tools=` schemas to the Qwen template; SFT intentionally omits them.
- Arena PR #47 carries deterministic per-replicate inference and environment seeds and verifies a game startup attestation. Kaetram-Open draft PR #333 centralizes server/common gameplay draws and rejects pre-seed draws, but it is not merged, deployed, or verified in a live run. Confirmatory launch therefore remains fail-closed.
- The factorial launcher now seals a portable, self-hashed prelaunch ledger, resolved prompts, exact pre-rewrite endpoint emissions, parsed tool-transition transcripts, state-boundary snapshots, launcher/results files, and an exact requested/completed-cell inventory that binds the prelaunch digest. Analysis must use the same clean registered commit and records source-file hashes. This prospective contract has not yet produced a live bundle.
- The future factorial now binds an outcome-blind executable analysis contract: seven exact paired-cluster formulas, paired Student-t inference, seven-way Bonferroni p-values and 99.2857% intervals, strict ±3-stage practical-relevance rules, paired d/g definitions, conservative zero-variance handling, a scipy 1.18.0 working-normal-model power calculation, and a no-replacement incomplete-run policy. This improves prospective validity but is not outcome evidence.
- The separate short-horizon local pilot did produce a complete retained bundle: 9/9 completed cells, hash-identified public weight blobs under one common Base tokenizer/patched renderer, paired inference/environment seeds, exact canonical-start projections, no logged `API error:` lines in retained stderr, and 261 rehashed files in sealed per-cell inventories. It records one prospective local execution only; it neither establishes independent reproducibility nor substitutes for the six-hour weights × recovery factorial.
- The preregistered 30-minute local factorial then completed all 18 Base/R2/R3 × recovery off/on cells. Its one-time export rehashed 1,266 files; anonymous exact CSVs and clarified tables are committed, while the complete report, receipt, and raw bundle remain local. The run is `legacy_v1_unattested`. Four cells advanced one stage; zero of 958 generations were malformed or recovery-eligible. The recovery manipulation therefore failed to engage, so this run does not identify a recovery effect and does not substitute for an independently powered confirmatory study.
- The trigger-incidence follow-up is checked in as a complete identity-scrubbed raw bundle. All 1,200 request rows, the frozen design, endpoint attestations, analysis, and manifests verify from a clean checkout. The producer verifier, an independently implemented outcome auditor, and a post-outcome seed-diversity audit agree. The latter found that all five nominal seeds within every state-condition group produced the same semantic response; rates and state-level contrasts survive collapsing, but the effective output grid is 20 states per cell and supports no stochastic uncertainty.
- The prospective state seeder restores persistent player state, not the complete shared world. Manuscript claims are therefore limited to player-state initialization unless NPC, mob, resource, concurrent-player, clock, and environment-RNG state are also sealed and restored.

No main-paper table should be labeled reproducible until a clean, documented environment regenerates it from a versioned artifact bundle.
